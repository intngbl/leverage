- enrollment = current_user.enrollments.find_by_campaign_id(@campaign.id)
- url = user_campaign_enrollment_path(@user.id, @campaign.id, enrollment.id)
= simple_form_for enrollment, url: url, method: :delete do |f|
  = f.submit "Leave", class: "btn btn-large btn-primary"

